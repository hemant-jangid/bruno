info:
  name: Get Transporter
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/spark/core/ewb/gettransporter?tranid=88AAECS4363H1ZA"
  headers:
    - name: gstin
      value: "{{gstin}}"
    - name: irp-api-username
      value: "{{irp-api-username}}"
    - name: irp-api-password
      value: "{{irp-api-password}}"
    - name: Octa-ApiKey
      value: "{{api_key}}"
  params:
    - name: tranid
      value: 88AAECS4363H1ZA
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
