info:
  name: Reset IMS Action
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/api/spark/core/gst/taxpayer/returns/ims?gstin={{gstin}}&action=RESETIMS&ret_period=012025"
  headers:
    - name: Octa-ApiKey
      value: "{{api_key}}"
  params:
    - name: gstin
      value: "{{gstin}}"
      type: query
    - name: action
      value: RESETIMS
      type: query
    - name: ret_period
      value: "012025"
      type: query
  body:
    type: json
    data: |-
      {
        "rtin": "33AXTPJ8438P2ZH",
        "reqtyp": "RESET",
        "invdata": {
          "b2b": [
            {}
          ]
          }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
