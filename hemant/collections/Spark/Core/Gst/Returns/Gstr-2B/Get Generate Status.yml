info:
  name: Get Generate Status
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/spark/core/gst/taxpayer/returns/gstr2b?gstin={{gstin}}&action=GENSTS2B&int_tran_id=032026222222222"
  headers:
    - name: Octa-ApiKey
      value: "{{api_key}}"
  params:
    - name: gstin
      value: "{{gstin}}"
      type: query
    - name: action
      value: GENSTS2B
      type: query
    - name: int_tran_id
      value: "032026222222222"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
