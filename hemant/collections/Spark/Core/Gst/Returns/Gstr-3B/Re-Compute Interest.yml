info:
  name: Re-Compute Interest
  type: http
  seq: 11

http:
  method: POST
  url: "{{base_url}}/api/spark/core/gst/taxpayer/returns/gstr3b?gstin={{gstin}}&action=CMPINT&ret_period=032025"
  headers:
    - name: Octa-ApiKey
      value: "{{api_key}}"
  params:
    - name: gstin
      value: "{{gstin}}"
      type: query
    - name: action
      value: CMPINT
      type: query
    - name: ret_period
      value: "032025"
      type: query
  body:
    type: json
    data: |-
      {
      	"gstin": "33AXTPJ8438P2ZH",
      	"ret_period": "032025"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
