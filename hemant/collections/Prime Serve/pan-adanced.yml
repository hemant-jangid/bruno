info:
  name: pan-adanced
  type: http
  seq: 1

http:
  method: POST
  url: https://api.primeserve.in/pan-api/fetch-advanced
  headers:
    - name: Apikey
      value: "{{Apikey}}"
  body:
    type: json
    data: |-
      {
       "pan_number": "AAGCR1762D",
       "consent": "Y"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
